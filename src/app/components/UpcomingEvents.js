import styles from './UpcomingEvents.module.css'; // Import CSS module

const UpcomingEvents = () => {
  return (
    <div className={styles.card}>
      <h2 className={styles.title}>Industrial visit</h2>
      <p className={styles.description}>
      Grab
      </p>
    </div>
  );
};

export default UpcomingEvents;